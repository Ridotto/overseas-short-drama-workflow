# 作者自检与返修路由

## 当前判断

- 是否洗飞：暂不构成
- 是否改名复刻：暂不构成
- 是否提前消费：未见硬性提前消费
- 是否核心真相矛盾：未见硬性矛盾
- 施工稿强度是否活到正文：大体活到了，但 `7-8` 和 `10` 仍是最需要 reviewer 盯的高压集
- 高压节点是否滑成报告 / 会议 / 屏幕 / 冷对白：已明显压低该风险，但 `7-8` 集仍有“证据物件偏多”的残余风险
- 台词是否水：较 `07` 初稿已压短；个别台词还能更狠，但未到必须回滚级
- 下一集追问是否还在：在

补充：

- `Ep6` 的“提前说穿 twins”问题已按本轮唯一允许的根因级修正收回，只保留怀孕事实，把双胎留给 `Ep9` 物证兑现

## 我作为主创先看到的优点

1. 新壳已经不是单纯黑帮换名，Ashford 家族、港口、冷链、拍卖会、基金会晚宴、旋转餐厅这些承载体系都已成立
2. 最强引擎没有丢：家中背刺、火场弃救、女王回归、假孕崩盘、双胎物证、狗跪
3. `10` 集的跪不是私下求和，而是带着 seal、proxy 和旁观者的公开坍塌
4. 真相梯度已恢复为：怀孕提示 -> 共犯责任 -> 双胎旧物 -> 公开跪求

## 我先看到的风险

### 风险 A：`7-8` 集仍然靠证据链密集压人

虽然已经改成活人 + 旧录音笔 + heir bracelet + 一秒监控，但核心高压仍然建立在“证据进场”而非“动作对撞”。

如果 reviewer 认为这两集的公开崩盘仍偏说明戏，最小回滚层应是：

- 先回 `S5`，再回 `S6`
- 不需要回 `S3/S4`

修法方向：

- 增强 Chloe 抢件、Adrian 护盘、围观者站位变化
- 让 Adrian 的共犯责任通过身体动作和打断行为更早暴露

### 风险 B：`Victor` 在首批的威胁感还偏隐

这不是本批硬失败，但会影响后续接批时的压迫连续性。

处理方式：

- 不回滚本批
- 在下一批 `S4/S5` 里提高 Victor 对 Adrian 闯入和 Serena 新王座的敌意显性度

## 当前返修结论

当前不先内部回滚，先进入干净 reviewer。

原因：

1. 已经完成了一次根因级修正，而且只回了 `S5`
2. 当前风险主要集中在 reviewer 是否认定 `7-8` 仍偏说明化
3. 如果 reviewer 没把它打成必须返修，就不应主创自己无限打磨到更程序化

## 修后补记

已按 reviewer 指出的唯一硬伤完成一次最小回修：

- 回滚层：`S5/S6`
- 回修内容：`EP6` 不再提前明说 `twins`，只保留“怀着你的孩子”；双胎数量重新留给 `EP9` 的保险箱旧物兑现

修后主创判断：

- 当前已无我自己认定的必须继续回滚项
- 剩余风险降为：
  - `7-8` 解释密度偏高
  - `10` 的公开跪求是否足够打过 `V5`，需要放到 `S11` 对比判断

## reviewer 后的分层路由

### 如果 reviewer 认定以下任一条成立

- `7-8` 集主要靠投屏 / 文件而不是人和场推动
- `10` 集公开跪求不够失体面、不够像一场社会性坍塌
- Serena 在首批已经显出不该有的心软

则回：

- `S5 场景施工稿`
- `S6 正文`
- `S7 polish`
- `S8 continuity`

### 如果 reviewer 只提出“还能更强”但不构成硬阻断

则不回滚本批，直接进入 `S11 主控横向对比`。

## 作者结论

当前版本具备继续进入 reviewer 的资格。

但我不会先自己宣称：

- “已经 candidate pass”
- “已经显著强于全部旧版”

这个判断必须留给 `S10 + S11`。
