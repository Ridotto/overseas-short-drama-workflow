# 决策与变更

## 2026-07-04：仓库治理收口，新 main 只保留当前产品链

- 用户确认：GitHub 上旧版本已经严重落后，后续应以当前 `shortdrama-remix` 主链替换 main；在推 GitHub 前，先把本地项目清理干净。
- 决策：当前 main 候选只保留新用户在 Codex 里直接使用产品所需内容：入口说明、PRD、主控、`source-import`、`short-drama-write`、短剧形态契约、必要 vendor 冻结来源和运行占位目录。
- 已清出当前工作树：旧输入样本、旧盲测、旧研究、`v3-executor-first/`、v20/v3 文件、`v2-restart/archive/`、`v2-restart/样本验证/`、`v2-restart/支撑审计/`、完整旧外部资产包和旧运行样本。
- 为避免损伤旧产物，旧内容已挪到本地忽略归档：`.local-archive/pre-main-replacement-2026-07-04/`。该目录不进入 GitHub。
- 新 main 的运行目录改为占位：`input/`、`shortdrama-remix/源本库/`、`shortdrama-remix/新剧/`。实际源本和新剧运行产物默认不提交。
- 注意：普通 commit + push 只会让 GitHub 当前文件树干净；旧内容仍在 Git 历史中。如需历史也彻底清掉，必须单独确认 orphan / history rewrite / force push。

## 2026-07-04：禁抄边界口径修正，不再机械禁词

- 用户指出：洗稿检查不应窄到“源本出现过的人名、短句、套路句，新稿就不能出现”。常见人名如 Claire / Jason，常见短句如 `don't touch me`、`go away`，以及好用的短剧套路句，本来就是类型工具，不应被当作硬失败。
- 决策：禁的是“可识别组合复刻”，不是字面词表。硬风险必须看罕见专名、独特道具、长句台词、场面调度、角色关系、事件顺序是否成组贴近源本。
- 已同步到 `shortdrama-remix/contracts/short_drama_form_lock_v1.md`、`skills/source-import/SKILL.md`、`skills/short-drama-write/SKILL.md` 和现有两个源本库的 `07_禁抄边界.md` / `handoff_to_short_drama_write.md`。
- 如果此前因为通用短句触发过局部台词返修，已改且可用的文本可以保留；但以后 clean reviewer 不得把通用短句单独列为条件项。

## 2026-07-03：V66 阿尔法样本书写层复验通过

- 新增验证目录：`v2-restart/样本验证/阿尔法样本/V66_alpha_v20_writer_layer_ep1_10_runner/`。
- 本轮不是完整 P0-P11 重跑，也不改变当前项目入口；只验证更新后的 P5/P6/P7 书写层能否在阿尔法样本 EP1-10 复现增强。
- 输入继承 V60 的 `P4 商业项目包 / 创作蓝图包`，不重写 P0-P4，不复刻商业平台稿，不新增 reviewer 规则。
- 产物包含：
  - `00_run_log.md`
  - `01_P5_scene_cards_EP1-10.md`
  - `02_EP1-10_正文修订稿.md`
  - `03_过不过结论.md`
- 主控结论：`pass_for_alpha_writer_layer_validation`。V66 修正了 V60 的主要书写层短板：EP8 付费兑现写满，EP9 保护链剥离从 proof-flow 变成现场位置 / 物件 / 控制权损失，EP10 二次失手有明确身体代价。
- 当前只证明“P4 商业项目包方向 + 新 P5/P6/P7 写作层”能在第二题材复现增强；不证明 v20 正式升默认，不证明跨所有题材稳定，不证明 clean reviewer 已独立审稿。
- 下一步如继续，应做 clean reviewer 或再跑一个非同型样本；不要在同一轮继续堆规则。

## 2026-07-03：直接接管 shortdrama-remix，停止外部方法摘要化

- 用户明确要求：不要再把外部项目抽成摘要、参考项、检查项或 workflow 字段，必须直接把可执行文件拿进当前项目并执行。
- 已将 `/Users/jiakun/Codex/自动化编剧-external-chain-rebuild/shortdrama-remix/` 原样复制到本仓库 `shortdrama-remix/`。
- 当前主链切换为：`source-import -> short-drama-write -> review`。
- 当前可执行入口：
  - `shortdrama-remix/skills/source-import/SKILL.md`
  - `shortdrama-remix/skills/short-drama-write/SKILL.md`
  - `shortdrama-remix/contracts/short_drama_form_lock_v1.md`
  - `shortdrama-remix/vendor/short-drama/SKILL.md`
  - `shortdrama-remix/vendor/short-drama/references/`
- `v3-executor-first/`、`workflow_spec_v20_产品契约编译候选版_2026-07-03.md`、`skill_chain_spec_v3_产品契约链候选版_2026-07-03.md` 降级为历史证据和失败对照，不再作为当前直跑入口。
- 当前已有样本随链路入库：
  - `shortdrama-remix/源本库/黑手党少主痛哭求原谅_前妻她杀疯了/`
  - `shortdrama-remix/新剧/He_Signed_Her_Death_Warrant/`
- 下一步不再修 workflow，而是在 `shortdrama-remix` 里直接执行写作链。若 EP5-8 仍偏证据戏、听证戏、文件戏，只能回到本地 `short-drama-write` 可执行文件或正文生产动作修，不得再从外层堆 workflow 摘要。

## 2026-07-03：v3 升级为全项目重做面，下一步跑 V63 全链

- 用户进一步明确：禁止只把外部方法变成检查项，禁止只修 workflow，整个项目重做。
- 本轮不新开第二入口，避免入口再次分裂；直接把 `v3-executor-first/` 从“后半执行器实验”升级为当前全项目重做面。
- 核心判断更新：V62 只证明 EP7-10 场面发动方向有价值，但不能证明完整项目通过；当前不再把“开 clean reviewer 审 V62 后半”作为下一步。
- v3 全项目重做生产链固定为：`R0 需求锁定 -> R1 源本赚钱力锁定 -> R2 新壳动作迁移 -> R3 用户可见商业成片包 -> R4 内部逐集生产包 -> R5 场面发动稿 -> R6 首批正文成片稿 -> R7 续批状态包 -> R8 主控横向商业判断`。
- 已新增 / 更新：
  - `v3-executor-first/00_当前唯一入口.md`
  - `v3-executor-first/07_全项目重做生产系统.md`
  - `v3-executor-first/08_用户可见商业成片包模板.md`
  - `v3-executor-first/09_内部生产包模板.md`
  - `v3-executor-first/10_V63标准首批运行协议.md`
  - `v3-executor-first/README.md`
- 当前执行口径：外部方法必须进入真实生产动作。`short-drama` 进入每集追看发动，`scene-writing` 进入角色行动冲突，`dialogue-subtext` 进入每句台词的攻击 / 遮掩 / 逼暴露，`oh-story` 只接源本锚点、状态追踪和去 AI 味。
- 当前下一步：按 `v3-executor-first/10_V63标准首批运行协议.md` 跑飞书三星样本 `V63_v3_full_rebuild_runner`，从 R0 到 R8 完整产出。最终只横向比较 V5 / 商业稿 / 成熟商业项目，不把 V60/V61/V62 当质量目标。

## 2026-07-03：禁止检查项化，启动 v3 executor-first 重做

- 用户明确否定继续“只把外部方法变成检查项 / 只修 workflow”的路线，要求整个项目按执行层重做。
- 新增隔离重做目录：`v3-executor-first/`。
- 本轮不改 `PRD_v4.md`，不删除 v20/v3 历史文件，不把 V60/V61 升默认。
- 当前核心判断：外部资产并不是没抄，文件层和 workflow 层都已接入；真正失败是执行层把 `short-drama`、`scene-writing`、`dialogue-subtext` 编译成了字段和检查项，没有变成写作时的生成动作。
- v3 新目标：把外部方法编译成 executor-first 写作动作机，核心从“字段齐不齐”改成“每一场能不能写出人压人、人反扑、人失控、人付代价”。
- 已新增：
  - `v3-executor-first/README.md`
  - `v3-executor-first/01_重做边界与产品契约.md`
  - `v3-executor-first/02_外部方法执行化编译.md`
  - `v3-executor-first/03_executor_first生产链.md`
  - `v3-executor-first/04_场面发动器协议.md`
  - `v3-executor-first/05_正文成片器协议.md`
  - `v3-executor-first/06_V62验证协议.md`
- V62 最小验证已落盘：`v2-restart/样本验证/飞书三星样本/V62_executor_first_scene_engine/`。
- V62 只基于 V61 P4，用 v3 场面发动器重做 EP7-10，再让正文成片器只吃新场面稿生成 EP7-10。
- 主控初判：V62 EP7-10 明显比 V61 后半更像真人对抗；继承椅、港徽、冷舱复盘、控制权转移不再主要靠系统机关自动结算，而是通过 Chloe / 董事会 / Adrian / Damian / Serena 的现场选择、反扑和代价落地。
- 限制：V62 只是最小验证，未执行 clean reviewer，未跑完整首批，不能升默认。
- 当前状态已被后一条决策更新：V62 只作最小正向证据，不再作为下一步主线；当前下一步改为 V63 全链标准首批。

## 2026-07-03：V61 飞书三星商业项目包生产范式隔离实验

- 新增 / 补齐验证目录：`v2-restart/样本验证/飞书三星样本/V61_feishu_commercial_package_runner/`。
- 本轮不改 `PRD_v4.md`，不改正式 workflow，不新增工位，只验证 `P4 商业项目包 -> P5 场景施工稿 -> P7 首批正文` 在飞书三星样本上是否继续增强。
- 本轮继承 V56 的 P0-P3，上游不重新发明；核心新增 P5-P11 与 run log。
- clean reviewer 已完成，结论为 `conditional_pass`。
- 比较口径修正：后续质量判断不再把上一版当主基准，避免局部爬坡；V56 只作为上游继承来源和辅助诊断，不作为质量目标。
- 横向判断只看 V5 和商业稿：V61 综合强于 V5 的项目正确性，但 EP7-10 仍偏系统权限 / 门禁 / 徽章 / 继承椅等机关式反转，真人对抗和反派反扑不足，不能声称稳定打平成熟商业稿。
- 限制：V60/V61 均为 `conditional_pass`，不能判正式 pass，也不能升默认 workflow。
- 下一步若继续，不应修全文或扩规则，而应开 V62，只小回 P5 的 EP7-10，把权限 / 徽章 / 继承椅 / 控制权反转前后压成真人对抗、反派反扑、公开关系撕裂和现场代价。

## 2026-07-03：V60 阿尔法样本商业项目包生产范式验证

- 新增验证目录：`v2-restart/样本验证/阿尔法样本/V60_alpha_commercial_package_runner/`。
- 本轮不从零重跑 P0-P3，而是继承既有上游，专门验证最新改动：`P4 商业项目包 / 创作蓝图包` 与 `P5 场景拆解层` 是否能保住 V5 体验信号并接近商业稿密度。
- 本轮完成 P0-P11，并留下 `00_run_log_V60_alpha_commercial_package_runner.md`。P10 clean reviewer 未读取作者自检，独立判定 `conditional_pass`。
- 横向结论按 V5 / 商业稿判断：V60 基本保住 V5 的抓人、升压、阶段释放和追看信号，但仍低于成熟商业稿密度。V60 修正了两处会削弱付费窗口的硬伤：EP7 不再提前回答胎儿存活；EP8-10 医疗抢人改为多层保护链被剥离，而不是 Keller 单人顺利偷走。
- 与 V5 体验信号对比：V60 基本保住开场抓人、压力升级、阶段释放 / 强悬置、结尾追看、可见承载和对立压力源；但 EP8 付费兑现略薄，EP9 仍有部分 proof-flow 感。
- 与商业稿密度对比：V60 已接近商业项目包的功能结构，但单集密度仍低于成熟商业稿，尤其 EP8-10 的公开失控、多方围攻和付费后强兑现不够满。
- 决策：保留当前最新 workflow 改动，不撤回；但不升默认。下一步不是修 V60，而是用同一生产范式跑另一个样本，验证是否仍能保住 V5 体验信号并接近商业稿密度。

## 2026-07-03：V56 源本锁 + 厚创作包 + 强桥段转译验证

- 新增验证目录：`v2-restart/样本验证/飞书三星样本/V56_source_locked_heavy_rewrite_runner/`。
- 本轮按当前 `workflow_spec_v20_产品契约编译候选版_2026-07-03.md` + `skill_chain_spec_v3_产品契约链候选版_2026-07-03.md` 跑飞书三星样本首批 1-10 集，并留下 `00_run_log_V56_source_locked_heavy_rewrite_runner.md`。
- 本轮不是浅洗 / 深洗二选一，而是按用户需求混合强度：背景、人物设定、行业场域和权力系统深度重构；源本情绪发动机、追看功能、付费冲动和答案顺序强锁；源本高辨识动作外形禁用。
- V56 采用港口冷链 / 家族信托壳，回收 V51 的厚创作包优势和 V55 的强桥段转译动作；核心做法是把源本强桥段拆成 `爽感功能 / 禁用外形 / 新壳等价动作`。
- clean reviewer 结论写入 `11_P10_clean_reviewer_轻版硬伤摘要.md`：`conditional_pass`。它判断 V56 没洗废源本，综合位置优于 V5 / V51 / V53 / V54 / V55 既有路线，但不能声称已打平成熟商业稿。
- P10 指出的四处 P7 局部问题已修，窄范围 patch check 写入 `11b_P10_patch_check.md`，结论为 `fixed`。
- 主控判断写入 `12_P11_主控交付判断.md`：V56 是当前飞书三星样本最佳候选，证明“推新但不洗废源本”的路线成立；但仍不能升默认 workflow，下一步应跑阿尔法样本验证跨题材稳定性，或只做 P7 商业稿密度 polish。

## 2026-07-03：深度洗稿强度提升到商业稿同级

- 本轮不改 `PRD_v4.md` 或样本文本。
- 决策：改写力度不是固定二选一，`贴近保留 / 等效迁移 / 深度重构 / 局部保留 / 删除修复` 可以按人物、背景、事件载体、关系动力、强节点和单集窗口混合使用，不能把整部剧卡死在某一个档位。
- 决策：如果用户要求更换背景、人物设定、行业场域或主要权力结构，一般应从 `深度重构 / 深度洗稿` 起步；凡是进入深度重构的部分，强度标准必须达到商业平台稿同级，不能因为洗得更干净而低于商业稿的短剧刺激。
- 已更新 `workflow_spec_v20_产品契约编译候选版_2026-07-03.md` 和 `skill_chain_spec_v3_产品契约链候选版_2026-07-03.md`：深度洗稿不再只是外形距离变量，而是 `源本情绪发动机 + 成片效果倒推 + 爆场面先行 + 人物欲望战 + 源本 / 新壳边界收束`。
- 深度洗稿允许大幅改事件载体、场面位置、作恶方式、证据出现方式、公开打脸场域、旧物形态和强节点拆分 / 合并 / 重排；但不得改用户收费边界、源本核心情绪发动机、关键答案顺序、新壳硬资产和非原创底线。
- 后续验证必须跑完整产物，至少对比 V5 / 商业稿，并看不同题材下是否仍能达到商业同级短剧刺激。

## 2026-07-03：V53 v20 asset patch 飞书三星样本验证

- 新增样本验证目录：`v2-restart/样本验证/飞书三星样本/V53_v20_asset_patch_feishu_runner/`。
- 本轮按 `workflow_spec_v20_产品契约编译候选版_2026-07-03.md` + `skill_chain_spec_v3_产品契约链候选版_2026-07-03.md` 跑飞书三星样本首批 1-10 集，并留下 `00_run_log_V53_v20_asset_patch_feishu_runner.md`。
- 本轮产物包含 P0-P11，其中 `P7` 为完整正文修订稿：`08_P7_首批正文修订稿.md`。
- P10 clean reviewer 本轮明确 `skipped`，没有伪造独立审稿，因此不能声称完整 workflow pass。
- 主控判断：V53 明显强于 V46，达到 `V5-level candidate`；在 EP7-10 的 proof-flow 控制和 EP10 新壳适配上强于 V5，但整体只能说不再明显输给 V5，不能证明稳定超过完整商业项目。
- 下一步如果继续，应先补 clean reviewer，再用同一套方法跑阿尔法样本；不要继续在飞书三星样本文本上局部修稿。

## 2026-07-03：PRD 对齐外部资产取舍审计与最小接入补丁

- 新增支撑审计：`v2-restart/支撑审计/2026-07-03_PRD对齐外部资产取舍审计.md`。
- 本轮修正一条错误判断：`oh-story` 是网文 / 小说写作包，不能作为短剧洗稿生产链，也不能再写成“分析旧爆款→重构→输出新剧本可 1:1 套架构”。
- 已更新 `v2-restart/参考资产/README_导航.md`：外部资产统一按 PRD 过滤，`short-drama` 只接短剧语法，`how-to-make-script` 只接 scene-writing / dialogue-subtext，`oh-story` 只接拆解 / 追踪 / 去 AI 味，`dramatron` 只作分层一致性启发，`shortdrama-pipeline` 只作状态与轻诊断启发。
- 已微调 `workflow_spec_v20_产品契约编译候选版_2026-07-03.md` 和 `skill_chain_spec_v3_产品契约链候选版_2026-07-03.md`：外部资产必须落到 P1/P3/P5/P7/P8 的真实生产动作，不允许作为知识库摘要、原创链、小说链、视频链或审核门进入主链。
- 本轮没有改 `PRD_v4.md`，没有改样本文本，没有新增工位，没有新增 reviewer / gate。

## 2026-07-03：V48 广域解法搜索，暂停把单一协议写入 workflow

- 本轮承认并修正上一阶段风险：只跑两次样本不足以证明 `P4.5`、`短剧发动稿` 或任何单一协议稳定，当前不得写入正式 workflow。
- 已开三条隔离审计并形成报告：`70_EXTERNAL_NATIVE_FLOW_AUDIT.md`、`71_MONEY_DRAMA_METHODS.md`、`72_LONG_SERIES_STABILITY.md`，主控整合为 `73_V48_BROAD_SEARCH_SYNTHESIS.md`。
- 外部项目补跑结果：`ReelForge-YAML` 在隔离环境下 `25 passed`，评测链可把 DeepSeek 样例从 `overall_score=0.832 / badcases=10` 优化到 `overall_score=0.95 / badcases=3`；它最值得借的是可量化 badcase 与 source traceability，不是镜头 YAML 产物形态。
- `duanju_create_agent` 在修正包路径和依赖后可跑通 mock 原生链，能落盘需求、计划、多集 K 线、人物、场景、正文、质量报告和 manifest；但 mock 正文质量低，不能照搬生成器，只能借张力 K 线、产物追责和自检结构。
- `0xsline/short-drama` 仍是最完整的 LLM skill 原生短剧链，适合开 `V48_external_native_runner` 手动按 `/start -> /plan -> /characters -> /outline -> /episode -> /review -> /export` 跑一轮，但它不是代码 runner，也不能照搬原创链路覆盖洗稿边界。
- 新的主判断：下一步不是改主链，而是在隔离实验里跑三组证据：外部原生链、ReelForge 式评测链、我们自己的源本锁洗稿候选链，并固定验证 EP1-3、首个付费前后窗口、EP21-24 三个窗口。
- 已进一步新建 `v2-restart/样本验证/飞书三星样本/V48_external_native_runner/`，按 `0xsline` 原生链受控生成 `creative-plan`、`characters`、37 集 `episode-directory`，并抽写 EP1 / EP10 / EP21。主控初判：外部链的“全剧方案 -> 人物体系 -> 全剧目录 -> 三窗口抽样”产物形态值得继续压测，但 clean review agent 超时未完成，不能视为独立审计通过。

## 2026-07-03：V47 源本锁 + 外部原生短剧执行隔离实验

- 本轮没有改 `PRD_v4`，没有改 `workflow_spec_v20`，没有继续修 V46 正文；新建隔离实验目录：`v2-restart/样本验证/飞书三星样本/V47_source_lock_external_native_experiment/`。
- 实验目标：验证外部短剧能力如果不再被二次翻译成内部字段，而是以“源本锁 + 新壳锁 + 短剧场面施工”的方式接入，是否能解决 V46 的 proof-flow 和短剧感不足。
- 已完成 A/B/C/D 四条分支：`0xsline-native`、`scene-writing-native`、`source-lock-adapter`、`V46-style patch`，并完成干净 reviewer 对比。
- 分支结论：`C source-lock-adapter` 最适合接入；`A` 的 episode 高压闭环可吸收；`B` 的 scene-writing 场面施工能力可吸收；`D` 旧 P-chain 局部修补路线不接。
- 进一步完成 R1 实验稿：`31_R1_CplusB_EP7-10_script.md`，使用 `C 源本锁 + B 场面施工` 写 EP7-10。
- R1 clean reviewer 结论：`conditional pass`。R1 明显比 V46 更有短剧现场，EP7-9 的答案顺序、人物锁、新壳资产和现场损失基本守住。
- 未通过点：EP10 的 `Blackwell 家族老枪` 作为权力遗物可接受，但 `Ethan 拿枪对准自己 -> 枪声 -> 黑屏` 不能进入正式 workflow，否则会把豪门商战新壳重新拉回源本枪口外形。
- 根因更新：P4 蓝图不是坏蓝图，真正断点是 `P4 创作蓝图包 -> 正文` 之间缺少很薄但很硬的 `P4.5 源本锁短剧适配卡`。
- 已完成 V47-R2：只替换 R1 的 EP10 终点，保留 `Blackwell 家族老枪` 作为权力遗物，删除实际枪声黑屏，把“一命换两命”转成 Blackwell 控制权、Ethan 经营权 / 继承权 / 自由 / 体面的不可逆死亡。
- R2 clean reviewer 结论：`conditional pass`。R2 比枪声版更适合正式 workflow，但高潮中 `确认终端 / 听证庭 / 死亡赔偿信托` 有流程化风险；正式链只能吸收“高危源本外形转译成新壳不可逆代价”，不能吸收成“法律终端按钮解决高潮”。
- 后续稳定性扩展修正了上一条口径：`P4.5` 不能当唯一解，只能降级为“源本 / 新壳边界收束能力”。
- 已新增阿尔法样本 `EP8-12` 跨题材压测：`P4.5 源本锁适配版`、`成片倒推版`、`人物欲望战版` 三种方法对比。
- 阿尔法压测结论：短剧体感最强的是 `成片倒推法`，长剧续航最好的是 `人物欲望战法`，洗稿稳定性最好的是 `源本锁适配法`；不能三选一，应组合为 `爆场面先行 + 人物欲望战驱动 + 源本边界收束`。
- 已完成阿尔法 `短剧发动稿 R1`：`60_ALPHA_SHORT_DRAMA_ENGINE_R1_EP8-12.md`。主控自判它明显强于单一 `P4.5` 版：更有可记忆爆场面、付费后兑现更快、EP12 对长剧续航更好。
- 当前仍不能承诺“稳定所有剧本 pass”。`短剧发动稿 R1` 尚未 clean review，也未跑第三样本；下一步应先审 R1，再做第三样本，不能直接升正式 workflow。

## 2026-07-03：V46 v20/v3 飞书三星样本跑通但未通过

- 本轮按 `workflow_spec_v20_产品契约编译候选版_2026-07-03.md` + `skill_chain_spec_v3_产品契约链候选版_2026-07-03.md` 跑飞书三星样本首批 1-10 集。
- 产物目录：`v2-restart/样本验证/飞书三星样本/PRD_v46_v20_product_contract_feishu_runner/`。
- 本轮完成 P0-P11，并留下 `00_run_log_V46_v20_product_contract_feishu_runner.md`。
- clean reviewer 口径已纠正：P10 未读取作者自检、旧样本、V5/V33/V40 或横向对比，不把历史半成品当模板。
- 结论：`workflow execution complete through P11 / creative outcome conditional revise / delivery outcome not pass yet`。
- 通过点：P4 创作蓝图包已成为完整用户可见主确认文档，包含故事梗概、人物设定、全剧粗纲、当前批集纲和状态禁区；上游不再碎片化。
- 通过点：源本 1-10 的主追感整体继承成立，新壳用港口冷链、Grey Harbor、Blackwell 继承人信托和家族权力场承载，不是简单改名复刻。
- 未通过点：EP7-8 仍有 proof-flow 回流；EP7 会计台词提前泄露 EP8 的血缘答案，导致 EP8 从答案揭晓降级为手续确认。
- 当前不升默认，不跑阿尔法样本；下一步不是改 workflow，而是只局部返修 EP7-8 的 P6-P7，再重新做 P10/P11。

## 2026-07-03：v20 样本前静态阻塞补丁

- 本轮不改 PRD，不跑样本，不新增 reviewer 或工位；只修 v20/v3 跑样本前会误导 runner 的静态边界。
- `workflow_execution_protocol_v1.md` 的当前 run log 模板已从旧 S0-S11 改为 P0-P11，避免执行记录把 v20 拉回 v19 语境。
- `workflow_spec_v20` 和 `skill_chain_spec_v3` 已明确：P4 高刺激集需要内部强度判断，但用户可见 `创作蓝图包` 只呈现最终戏剧承载，不展示内部候选演法表。
- P5 边界改为：项目事实输入只读 P4；方法资产必须加载，但只提供写法协议，不能引入新剧情事实、角色关系、洗稿边界或付费排程。
- P6 边界改为：只负责忠实执行 P5，不承担独立验证源本级洗飞 / 复刻；源本级检查由 P9/P10/P11 对照 P1-P5 与最终正文承担。
- 当前状态仍是候选 workflow，尚未升默认；静态检查通过后，下一步才是按 v20/v3 跑飞书三星样本并留下 P-chain run log。

## 2026-07-03：workflow v20 产品契约编译候选版落地

- 本轮使用 `gstack-spec`、`grill-with-docs`、`project-workflow` 口径处理，但不创建 GitHub issue、不讨论工程实现、不跑样本。
- 新增 `v2-restart/workflow_spec_v20_产品契约编译候选版_2026-07-03.md`，从 `PRD_v4_产品契约_spec_v2.md` 重新编译，不直接续改 v19。
- 新增 `v2-restart/skill_chain_spec_v3_产品契约链候选版_2026-07-03.md`，作为 v20 的配套执行链说明，不是最终 `.codex/skills` 目录设计。
- v20 的核心修正：用户可见主链只保留需求确认、源本有效性摘要 + 洗稿方案摘要、创作蓝图包、首批正文修订稿和反馈回流；场景施工稿、reviewer、run log 不再出现在用户主链。
- 创作蓝图包固定为商业项目形态：故事梗概、人物设定、全剧粗纲、当前批集纲、当前批状态与禁区说明。
- 内部生产链改用 `P0-P11` 产物链表达，避免把 v19 的 S 编号继续当产品事实；P5 只读 P4，P6 只读 P5，P7 只读 P6。
- 外部协议的接入位置已固定：`paywall-design` 进入 P1-P4 的追付费机制，不在 P5 临场补逻辑；`/episode + scene-writing` 进入 P5；`dialogue-subtext` 进入 P7。
- 已完成静态检查：创作蓝图形态、用户主链、输入隔离、外部协议接入、不得默认付费点、reviewer 只兜底全部通过。
- 已将 v19 和 skill_chain_v2 移入 `v2-restart/archive/superseded-v19-2026-07-03/`，避免根目录同时保留两个当前候选。
- 当前下一步：按 v20 先跑飞书三星样本；通过后再跑阿尔法样本。v20 尚未升默认。

## 2026-07-03：当前入口去重与旧状态文件归档

- 当前项目入口只保留一条：`AGENTS.md` 指向 `v2-restart/当前工作入口.md`，再进入 `项目基础说明.md`、`PRD_v4.md`、`specs/PRD_v4_产品契约_spec_v2.md`。
- `v2-restart/当前状态_2026-07-01_重收口.md` 已移入 `v2-restart/archive/restart-notes/`，只作为 v16/v19 历史收口记录，不再作为新线程或新 agent 入口。
- 根目录下的当前状态以 `v2-restart/当前工作入口.md` 为准；原 `workflow_spec_v19` 已移入 `v2-restart/archive/superseded-v19-2026-07-03/`，只作为反查对象，不作为当前候选或直接续跑入口。

## 2026-07-03：PRD_v4 产品契约 spec_v2 重写

- 本轮不改 `PRD_v4.md`，不继续修 v19 / skill_chain，不跑样本；新建 `v2-restart/specs/PRD_v4_产品契约_spec_v2.md`。
- `spec_v2` 以 `PRD_v4.md` 为主上游，以 `v2-restart/项目基础说明.md` 为地基约束；商业项目只校准中间文档形态，外部项目只校准写法协议。
- `PRD_v4_执行系统_spec_v1.md` 降级为变形反例：它覆盖了 PRD，但把产品契约写偏成执行合同、gate、打回、reviewer 和 S 编号语境。
- `spec_v2` 的核心修正：主体是产品契约，包含用户可见产物契约、内部生产产物契约、自然承接关系、内建质量责任；最后只保留很薄的 workflow 编译附录。
- 创作蓝图包被明确为正文前的用户主确认文档，必须按商业项目形态组织为：故事梗概、人物设定、全剧粗纲、当前批集纲、当前批状态与禁区说明。
- 内部产物被明确为支撑产物：源本有效性摘要、洗稿方案摘要、故事控制包、场景施工稿、连续性状态和 reviewer 摘要都不得替代用户主文档。
- 质量责任改为压进产物定义：不能依赖末端 verifier、无限打回或 reviewer 来生产短剧感。
- 已同步更新 `AGENTS.md` 和 `v2-restart/当前工作入口.md`：当前下一步不再是直接跑 v19，而是先从 `spec_v2` 重新编译下一版 workflow / skill chain，再做静态检查和样本验证。

## 2026-07-03：PRD_v4 全量编译为执行系统 spec

- 本轮不改 `PRD_v4.md`，不直接修 v19 / skill_chain，不跑样本；只把 `PRD_v4` 全量编译为仓库本地执行系统 spec。
- 新增主产物：`v2-restart/specs/PRD_v4_执行系统_spec_v1.md`。
- 根因判断更新：当前问题不是单个蓝图字段、paywall 或 reviewer 规则缺失，而是 `PRD_v4 -> workflow` 的编译层被 v19 / skill_chain / 历史补丁污染，导致用户可见产物、内部执行附录、质量 gate 和 reviewer 语言混在一起。
- 新 spec 明确：`PRD_v4` 是唯一上游；v19 / skill_chain / 历史样本都只能作为后续反查对象，不能反向污染 spec。
- 新 spec 已覆盖 PRD 第 1-19 节，并包含 traceability matrix、产物流、读取边界、执行 owner、冻结回滚、失败类型、reviewer 边界、质量门、workflow 升默认门槛和后续迁移规则。
- 本轮按 `gstack-spec`、`grill-with-docs`、`project-workflow` 三个口径完成挑字：删除了旧讨论偷渡进来的 `paywall / 收费` 偏置，并把 `三档候选 / proof-flow / gate / run log / reviewer checklist` 限定为内部附录或检查项，禁止进入用户可见主文档。
- 下一步不是跑样本，而是用该 spec 反查 v19 / skill_chain，列出冲突清单并清理；静态检查通过后再跑 `S0-S4` 产物形态验证。

## 2026-07-03：V41 根因回收与 v19 强戏候选不变量修正

- 本轮没有改 PRD，没有新增工位，没有继续修样本文本；只继续收窄 `v19 + skill_chain_v2` 的生产合同。
- V41 的硬症状集中在 `EP7-8`：链路能成戏，但公开拆穿窗口又滑回 proof-flow；`EP8` 还提前吃掉 `EP9` 的一部分定锤。
- 根因不是单纯缺 `paywall`，也不是 reviewer 不够狠；真正回归点是：v19 写作内核替换后，把 V32 已验证有效的强戏候选不变量冲淡了。
- V32 的有效不变量是：证据、文件、屏幕、系统只能做触发器；高压节点本体必须是人物在现场被迫行动，并造成关系、权力、体面、身体或空间上的可见后果。
- 本轮修正：
  - `S3` 增加高价值节点 `观看冲动总线 / 最低强度地板 / proof-flow 风险标记`；
  - `S4` 恢复 `冷安全候选 / 新壳自然候选 / 高冲动候选` 三档候选和淘汰理由；
  - `S5` 增加 proof-flow 硬门：证据只能触发动作，不能被 scene-writing 包装成高压戏本体。
- 下一步验证方式：用干净 sub-agent 按当前 `v19 + skill_chain_v2` 重跑飞书三星样本首批 `1-10` 集，重点看 `EP7-9` 是否从“证明流程”恢复成“人物被迫行动后的不可逆后果”。

## 2026-07-02：v19 写作内核替换

- 本轮没有改 PRD，没有新增工位，没有继续修样本文本；只原位更新 `v2-restart/workflow_spec_v19_创作蓝图包前置候选版_2026-07-02.md` 和 `v2-restart/skill_chain_spec_v2_创作蓝图链候选版_2026-07-02.md`。
- 本轮没有改 PRD，没有新增工位，没有继续修样本文本；只更新现行 workflow / skill 合同，并冻结外部协议原文。
- 根因重判不变：不是不会写，而是当前写作内核过于自创，导致 `S3/S4/S5` 的强度传递不稳。
- 当前修法不再继续自造抽象协议，而是直接替换内核：
  - `S3/S4` 直接采用 `0xsline /outline` 的分集与节奏骨架；
  - `S5` 直接采用 `0xsline /episode + scene-writing`；
  - `S6/S7` 直接采用 `dialogue-subtext`。
- 保留不动的项目边界：`S1` 源本有效性、`S2` 洗稿边界、`S8` continuity / DNC / callback-payoff、用户收费边界优先、源本原节奏优先、全剧粗当前批细。
- `commercial-script` 本轮明确排除，不进主链。
- 以后实现与 review 一律只认本地冻结版本，不再靠远端临时读取或二手摘要。

## 2026-07-02：v19 创作蓝图包前置候选版定稿

- 本轮未新开 PRD 版本号，原位更新 `v2-restart/PRD_v4.md`。
- 本轮新增候选执行细则：`v2-restart/workflow_spec_v19_创作蓝图包前置候选版_2026-07-02.md`。
- 本轮新增工位链说明：`v2-restart/skill_chain_spec_v2_创作蓝图链候选版_2026-07-02.md`。
- v19 的核心变化不是补短剧规则，而是把中间层重构成三层：`故事控制包 -> 创作蓝图包 -> 场景施工稿`，再进入正文。
- 用户可见主链改为：`需求确认 -> 源本有效性摘要 + 洗稿方案摘要（一次前置确认） -> 创作蓝图包（用户确认） -> 首批正文修订稿 -> 用户反馈回流`。
- v19 固定四种模式：`诊断/蓝图模式`、`标准首批模式`、`长篇多批次模式`、`局部改稿/定点返修模式`。
- 关键层硬门槛固定为 `S4/S5/S6/S8`，责任是 `工位自检 + 主控 gate`；过线证据必须写在产物头部，不只写进 `run log`。
- v19 仍是候选版，当前不切换 `当前工作入口.md` 的默认入口；必须先过两样本验证，才允许升为默认 workflow。

## 2026-07-02：V27 v16 workflow 样本验证结果

- 本轮按 `workflow_spec_v16_全能力交接候选版.md` 跑飞书三星样本首批 1-10 集，产物目录：`v2-restart/样本验证/飞书三星样本/PRD_v27_v16workflow/`。
- S0-S11 已完成，包含 run log、作者自检、两轮干净 reviewer 和主控横向对比。
- 结论：`workflow execution complete through S11 / creative outcome revise / delivery outcome not pass yet`。
- 关键结果：v16 的交接链比 V25 更干净，修掉了 EP10 枪口黑屏和“听见怀孕仍离开”重复消费的硬阻断；但 V27 仍未 pass，中后段证据播报感、Celeste 抵抗不足、EP6/EP9 真相分配和 EP10 台词软化风险仍需返修。
- 当前不正式化 v16，不继续把 run log 当质量证明，不把 reviewer 当质量发动机。

## 2026-07-02：V28 深度重构变量验证

- 本轮继续使用 `workflow_spec_v16_全能力交接候选版.md` 和同一飞书三星样本，只把改写力度改为“深度重构”，产物目录：`v2-restart/样本验证/飞书三星样本/PRD_v28_v16_deep_rewrite/`。
- 本轮完成 S0-S9 与 S11，S10 干净 reviewer 未运行；因此不能声称完整 reviewer 通过。
- 结论：`workflow execution complete through S9 + S11 / creative outcome revise / delivery outcome not pass`。
- 关键结果：深度重构显著降低了“改名复刻 / 旧外形回流”风险，EP1-EP3 的发布会、吊篮、坠海、医疗船失子比 V27 更有视觉冲击；但 EP6-EP8 仍偏连续证据链，Serena 后半段反扑燃料偏薄，EP10 仍有机构按钮残影。
- 当前判断：深度重构有效解决“外形距离”，但不是“短剧场面发动”的总解，不能直接设为万能默认；后续若修 workflow，重点仍是 S3/S4/S5 如何把真相和证据转成现场行动与人际冲突。

## 2026-07-02：v16 生产链运行约束小修版

- 基于 `v2-restart/生产链能力执行对账_2026-07-02.md`，新增候选 spec：`v2-restart/workflow_spec_v16_生产链运行约束小修版_2026-07-02.md`。
- 本轮没有改 PRD，没有新增 reviewer 规则，没有继续修 V28 正文。
- 小修思路：把 6 个生产链假执行点合并成 3 个运行约束：S3 `追看强度地图`、S4 `场面承载选择`、S5 `writer 行动链 / 戏的粗稿`。
- 已完成静态 review：`v2-restart/workflow_spec_v16_生产链运行约束小修版_静态review_2026-07-02.md`。
- 当前判断：静态上可进入样本验证，但未跑新样本，不能正式化。下一步如继续，应使用同一飞书三星样本跑首批 1-10 集，并横向对比 V5 / V22 / V24 / V25 / V28。

## 2026-07-02：V29 小修版生产链样本验证

- 本轮使用 `workflow_spec_v16_生产链运行约束小修版_2026-07-02.md` 跑同一飞书三星样本首批 1-10 集，产物目录：`v2-restart/样本验证/飞书三星样本/PRD_v29_v16_runtime_constraints/`。
- 本轮保持 V28 的深度重构力度，只验证 S3 `追看强度地图`、S4 `场面承载选择`、S5 `writer 行动链 / 戏的粗稿` 是否能进入生产链。
- 本轮完成 S0-S9 与 S11，S10 干净 reviewer 未运行；因此不能声称完整 reviewer 通过。
- 结论：`workflow execution complete through S0-S9 + S11 / creative outcome conditional revise / delivery outcome not pass yet`。
- 关键结果：V29 相比 V28，EP6-EP9 更稳定地把证据和真相转成现场动作：旧训练水槽重演选择、Serena 现场走危险路线、Ethan 亲手摘继承救生徽、手动拉开旧保温箱。说明小修方向有效，但 EP10 卡点和 Nora 情绪余韵仍需用户 / reviewer 检查后再决定是否返修。
- 当前不正式化小修版，不把 run log 当质量证明，不把 V29 当交付稿。

## 2026-07-02：V30 clean sub-agent 小修版验证

- 用户指出 V29 由主线程直接跑，不能算干净 workflow 验证；本轮新开干净 sub-agent，使用 `fork_turns=none`，禁止读取 V29 目录，按 `workflow_spec_v16_生产链运行约束小修版_2026-07-02.md` 重跑同一飞书三星样本首批 1-10 集。
- 产物目录：`v2-restart/样本验证/飞书三星样本/PRD_v30_clean_v16_runtime_constraints/`。
- 本轮完成 S0-S9 与 S11，S10 reviewer 按要求跳过；clean runner 声明未读取 V29 文件。
- 结论：`execution complete / creative outcome promising revise / delivery outcome not pass`。
- 关键结果：V30 独立生成了 `wrong door / old lab / rescue key / safety chamber` 的新承载链，EP6-EP10 比 V28 更像逐拍场面而非证据报告，说明 S3/S4/S5 生产动作方向在干净 agent 中也能生效。
- 仍未通过：EP7/EP8 仍有终端/权限材料偏冷风险，EP10 安全舱后续兑现会影响 Ava 正当性，后续反派燃料需补派系/Marcus/资产压力。当前不正式化 workflow，不把 V30 当交付稿。

## 2026-07-02：v17 0xsline /episode 写作工位接入候选版

- 本轮判断：现成项目不能整套直接接入本项目，因为 0xsline 是原创短剧工作台，而本项目是洗稿工具；但 0xsline 的 `/episode` 写作动作可以直接接入 S5/S6。
- 新增候选 spec：`v2-restart/workflow_spec_v17_0xsline_episode写作工位接入候选版_2026-07-02.md`，保留 v16 主链，只改 writer 生产方式。
- 核心改动：S5 必须按 0xsline `/episode` 的阶段加载方式运行，在写当前集 / 场景组时使用 `opening-rules`、`rhythm-curve`、`hook-design`、`satisfaction-matrix`，并按需使用 `paywall-design`、`villain-design`、`genre-guide`。
- 新增 S5 `强戏演法选择`：高价值节点至少比较 2-3 个演法，判断哪一个更能制造观看冲动、哪一个太冷、哪一个太像源本，选定后再进入 S6。
- S6 被限制为执行 S5 已选定的强戏演法；如果正文只完成剧情功能但没有制造观看冲动，回 S5，不交给 reviewer 兜底。
- 当前仍未正式化 v17，下一步必须用同一飞书三星样本首批 1-10 集重跑，并与 V5 / V22 / V24 / V25 / V28 / V30 横向对比。

## 2026-07-02：V31 clean v17 /episode 接入验证

- 本轮新开干净 runner，使用 `workflow_spec_v17_0xsline_episode写作工位接入候选版_2026-07-02.md` 跑同一飞书三星样本首批 1-10 集。
- 产物目录：`v2-restart/样本验证/飞书三星样本/PRD_v31_clean_v17_0xsline_episode/`。
- 本轮完成 S0-S9 与 S11，S10 reviewer 按测试范围跳过；因此不能声称 reviewer pass。
- 主控对比文档：`v2-restart/样本验证/飞书三星样本/PRD_v31_clean_v17_0xsline_episode/12_V31_vs_V5_V30_商业平台稿_主控对比.md`。
- 结论：V31 明显强于 V30，说明 0xsline `/episode` 接入 S5/S6 有效；但 V31 仍未超过 V5 和商业平台稿的短剧本能刺激。
- 根因更新：不是 writer 完全不会写，而是 `/episode` 的强戏选择发生得太晚；到 S5 时，S4 给出的候选演法空间仍偏冷、偏机构、偏制度。下一步若继续修，应把“观看冲动 / 爽感类型 / 本能刺激承载”前移到 S3/S4，而不是继续加 reviewer 或只加 S5 规则。

## 2026-07-02：v18 观看冲动与强戏候选前移候选版

- 新增候选 spec：`v2-restart/workflow_spec_v18_观看冲动强戏候选前移版_2026-07-02.md`。
- 本轮没有改 PRD，没有继续修 V31 正文，没有新增 reviewer 规则，也没有整套接入 0xsline。
- 根因判断：V31 证明 S5/S6 接入 0xsline `/episode` 有效，但强戏选择发生太晚；如果 S3/S4 给到 writer 的候选承载仍偏冷、偏机构、偏制度，writer 只能在冷材料上加热。
- v18 改法：S3 新增 `观看冲动总线 / 爽感类型 / 强戏候选边界`，把高价值节点从“剧情发生什么”前移成“观众为什么想追、爽感债务是什么、最低强度地板是什么”。
- v18 改法：S4 新增 `强戏候选空间`，高价值节点必须给出冷安全候选、新壳自然候选、高冲动候选；如果候选全部偏机构 / 系统 / 文件 / 会议，S4 不能放行。
- v18 保留 v17 的 S5 `/episode` 写作动作，但 S5 不再从零发明强戏候选空间，而是在 S4 候选空间里选择、重组、发动成戏。
- 当前仍未正式化 v18。下一步如果继续，应新开干净 runner，用同一飞书三星样本首批 1-10 集验证，并与 V5 / V22 / V24 / V25 / V28 / V30 / V31 横向对比。

## 2026-07-02：V32 clean v18 强戏候选前移验证

- 本轮新开干净 runner，使用 `workflow_spec_v18_观看冲动强戏候选前移版_2026-07-02.md` 跑同一飞书三星样本首批 1-10 集。
- 产物目录：`v2-restart/样本验证/飞书三星样本/PRD_v32_clean_v18_impulse_candidates/`。
- 本轮完成 S0-S9 与 S11，S10 reviewer 按测试范围跳过；因此不能声称 reviewer pass。
- 主控横向对比文档：`v2-restart/样本验证/飞书三星样本/PRD_v32_clean_v18_impulse_candidates/12_V32_vs_V5_V30_V31_商业平台稿_主控横向对比.md`。
- 结论：V32 明显强于 V31 / V30，说明 v18 的 `观看冲动总线` 和 `强戏候选空间` 前移有效；S3/S4 不再只交剧情功能，S5/S6 拿到更热的候选材料。
- 仍未通过正式化：V32 仍未稳定打过 V5 / 商业平台稿的原始短剧刺激，E5/E6 仍有轻微机构化 / 信息检索风险。
- 当前判断：v18 是目前最强候选方向，但不是 candidate pass。下一步不应继续加规则，应先做干净 reviewer 或用户侧直接读正文，判断 V32 是否已经达到“短剧强戏”体感。

## 2026-07-02：V40 阿尔法样本前半链验证（主线程接管）

- 原计划用单个干净 subagent 跑第二样本前半链 `S0-S4`，但执行器连续出现 `running` 无产物、目标目录空白、最近无文件修改的假运行状态，因此本轮中止 subagent 路径，改由主线程直接落地。
- 产物目录：`v2-restart/样本验证/阿尔法样本/PRD_v40_clean_v19_alpha_s0s4_runner/`。
- 本轮只完成 `S0-S4`，包含：`00_run_log`、`01_需求确认摘要`、`02_源本有效性摘要`、`03_洗稿方案摘要`、`04_故事控制包`、`04b_二次需求确认结论`、`05_创作蓝图包`。
- 关键结果：第二样本的中间承接层已真实落地，不再是空的计划话术；`S4` 已经是 prose 戏稿蓝图，没有退化成 `hook/spike/button` 或“本集功能”任务单。
- 当前判断：`v19` 在第二样本上至少证明了“分析 -> 控制 -> 蓝图”这半条链能落成文件，且新壳没有滑回狼人 / 命定伴侣 / 地下赌场原外形；但尚未验证 `S5-S11`，更未验证正文质量。
- 下一步不应回去继续追 subagent 假运行，而应基于这批 `S0-S4` 产物继续推进 `S5-S11`，再与 `V5 / V33 / 商业平台参照` 做横向判断。

## 2026-07-02：V40 阿尔法样本完整首批验证（批次边界纠正后）

- 在同一目录 `v2-restart/样本验证/阿尔法样本/PRD_v40_clean_v19_alpha_s0s4_runner/` 内，已继续完成 `S5-S11`，补齐：
  - `06_场景施工稿_首批1-10集.md`
  - `07_screenplay正文初稿_首批1-10集.md`
  - `08_台词视听polish稿_首批1-10集.md`
  - `09_story_memory_checkpoint.md`
  - `10_作者自检与返修路由.md`
  - `11_reviewer_干净审稿.md`
  - `12_主控横向对比与拍板.md`
- 本轮最重要的中途修正不是台词，而是把首批 `1-10` 从“偷吃到公开认领 / 关系决裂”收回到源本真实前 10 集位置：`原罪夜 -> 怀孕债 -> 误判 -> 黑房危机 -> 救场 -> 孩子坐实 -> 转运危机`。
- 结论：`v19` 在第二个不同题材样本里，仍能把 `S4 -> S5 -> S6` 落成戏，不会自动滑回任务单和说明书；但本轮 `S10` 由于 subagent 执行器假运行，只能由主线程按协议代行，因此不能算真正的 clean reviewer 证明。
- 当前主控 verdict：`workflow execution complete / creative outcome promising but not pass / delivery outcome not pass yet / v19 second-sample status direction confirmed, not promotable yet`。
- 这轮的真正价值不是“已经打过 V5”，而是：`v19` 不再只是飞书三星样本里的特例；它在第二个题材上也能保持中间承接层成戏，并守住源本首批付费点位置。

## 2026-07-02：V40 vs V33 / V5 / 商业平台稿 读感对比

- 新增对比文档：`v2-restart/样本验证/阿尔法样本/PRD_v40_clean_v19_alpha_s0s4_runner/13_V40_vs_V33_V5_商业平台稿_读感对比.md`。
- 结论不是“V40 写坏了”，而是：`V40` 证明 `v19` 已能在第二个不同题材里继续把中间层接成戏；但在首批商业刺激上，当前排序仍是：`商业平台稿 > V5 > V33 > V40`。
- 这轮对比把问题进一步收窄：现在的主要矛盾已不再是“writer 拿任务补戏”或“中间产物又散了”，而是 workflow 还没有把**不同源本首批的商业刺激度下限**稳定校准到足够高。
- 更直接地说：`v19` 现在更擅长保住源本首批真实节奏，但还不够擅长在不洗飞的前提下，把不同源本的首批都逼近 `V5 / 商业平台` 那种“立刻想往下追”的强度下限。

## 2026-07-02：项目治理底座标准化

- 本项目已正式初始化为本地 git 仓库，默认分支为 `main`。
- 已新增根目录 `.gitignore`，忽略本地 session 噪音、hook 日志、系统文件与环境文件。
- 已新增根目录 `README.md`，用于对外说明项目定位、入口、目录结构和当前阶段判断。
- 已对两处明显不适合直接入库的飞书 token / 附件 token 做脱敏处理。
- 已完成首次基线提交：`d4d85b6 chore: initialize repository and capture workflow baseline`
- 已创建并推送 GitHub 私有仓库：
  - [Ridotto/overseas-short-drama-workflow](https://github.com/Ridotto/overseas-short-drama-workflow)
- 当前判断：项目管理底座已经进入“可回滚、可分支、可 diff、可远程备份”的正常状态。后续 workflow / 样本验证 / root cause 调整，应基于 git 分支推进，不再继续在无版本基线状态下讨论。

## 2026-07-02：迁线程防失忆包落地

- 已更新 `v2-restart/当前工作入口.md`，当前入口不再指向 `v16`，而是明确收束到：项目治理底座完成、`v19` 两样本验证后、当前主要矛盾是“首批商业刺激下限责任缺位”。
- 当时曾更新 `v2-restart/当前状态_2026-07-01_重收口.md` 顶部补丁；该文件已在 2026-07-03 移入 `v2-restart/archive/restart-notes/`，不再作为当前入口。
- 已新增迁线程包目录：`v2-restart/thread_handoff_2026-07-02/`
- 当前 handoff 包包含：
  - `00_先读这个.md`
  - `01_当前任务.md`
  - `02_已验证结论.md`
  - `03_失败模式黑名单.md`
  - `04_当前根因与未解问题.md`
  - `05_新线程启动prompt.md`
  - `06_导演反馈与反复踩坑.md`
  - `07_关键证据索引.md`
- 目的不是搬运整段旧对话，而是把真正值钱的项目真相、失败模式、已验证结论、当前根因和下一步任务固化进仓库文件，让新线程按文件接手，而不是按长聊天记忆接手。

## 2026-07-03：V53 clean reviewer 与 V54 深度洗稿实验

- V53 已由干净 reviewer 独立审稿，结果写入：`v2-restart/样本验证/飞书三星样本/V53_v20_asset_patch_feishu_runner/13_clean_reviewer_独立审稿.md`。
- V53 reviewer verdict：`fail`。主要硬伤不是单纯刺激不够，而是 EP7-8 五年后仍以“孕妇 / 未出生继承人”作为继承权核心，时间线不成立；其次 EP4-8 仍偏制度、权限、认证、账户，EP10 没有压过 V5 / 商业稿的直接刺激。
- 新增 V54 深度洗稿实验：`v2-restart/样本验证/飞书三星样本/V54_v20_deep_wash_runner/`。本轮按 v20 从源本重跑，实验新壳为拉斯维加斯博彩帝国 / 博彩清算网络 / 赌场信誉战。
- V54 运行链路已落盘 `run_log`、`P0-P9`、`P11` 与 `MANIFEST`；P10 clean reviewer 按任务范围跳过，因此 V54 不能声称 reviewer pass。
- 当前主控判断：V54 的短剧刺激、直接爽点和追看冲动强于 V53，局部接近或超过 V5；但边界干净度弱于 V53，尤其 EP10 “一命换两命 / 枪响”与源本强节点外形同构偏高。
- 当前排序只能暂定为：短剧刺激 `V54 >= V5 >= V53`，洗稿边界干净度 `V53 >= V5 > V54`，综合 `V5 ≈ V54 > V53`。V54 仍不能宣称稳定超过 V5，更不能宣称打平成熟商业稿。
- 根因更新：不是 v20 大结构彻底失效，而是“强刺激写法”和“洗稿边界”仍未被同一层稳定同时满足。V53 偏干净但冷，V54 偏刺激但贴源本；下一步若继续，应先让干净 reviewer 审 V54，再决定是回 P4 重设强节点承载，还是回 P5 把证据戏动作化。

## 2026-07-03：V55 强桥段转译小实验

- 新增隔离实验目录：`v2-restart/样本验证/飞书三星样本/V55_strong_scene_translation_experiment/`。
- 本轮不是完整 workflow 重跑，只验证一个问题：源本强桥段能否拆成“爽感功能 / 禁用外形 / 新壳等价动作”，从而避免在 V53 的冷和 V54 的贴源本之间来回摆。
- 本轮只处理 V54 的 EP7-10：把源本式“验血、保险柜遗物、枪响自杀”替换为赌城壳原生的“继承黑筹码 / C-7 事故复盘 / 自锁隔离倒计时”。
- 当前主控判断：V55 方向上比继续全链路重跑更有价值；它可能同时提高刺激和边界干净度，但尚未经过 clean reviewer，不能判 pass，也不能升 workflow。
- 下一步若继续，应让干净 reviewer 专审 V55 EP7-10：一看短剧刺激是否保住，二看洗稿边界是否比 V54 干净。

## 2026-07-03：强桥段转译能力根因研究

- 新增研究文档：`v2-restart/支撑审计/2026-07-03_强桥段转译能力根因研究.md`。
- 研究结论：不要继续 V56/V57 式试稿循环；当前核心缺口是 `P2/P4/P5` 之间没有稳定冻结“源本强桥段爽感功能 -> 禁用源本动作外形 -> 新壳原生强动作 -> 现场损失 -> 下一债”的创作决策。
- 关键证据：V5 写作前工作稿曾经有有效的“源本体验功能 / 禁止外形 / 新冲突动作链 / 权力关系变化 / 观众直接看到什么 / 卡点”动作迁移卡；后续 v20 继承了原则，但没有把这张卡变成生产必需能力。
- 下一步方向：先设计最小机制补丁，把“强桥段转译卡”并入 P2，把“新壳原生强动作”并入 P4 当前批集纲，把“无强动作不可施工 / 源本外形回流不可继续写”并入 P5 输入完整性；用户确认后再改 workflow，不再先跑样本。

## 2026-07-03：强桥段转译机制补丁接入 v20 / v3

- 已按根因研究结论，最小修改 `workflow_spec_v20_产品契约编译候选版_2026-07-03.md` 与 `skill_chain_spec_v3_产品契约链候选版_2026-07-03.md`。
- 本轮不改 `PRD_v4`，不新增 P 编号，不新增 reviewer，不新增工位，不把 V55 判为通过样本。
- 机制补丁内容：
  - P2 `洗稿方案摘要` 必须完成 `强桥段转译卡`：源本强节点为什么爽、禁用哪些源本动作外形、新壳用哪个原生强动作、谁被迫行动、谁抵赖反咬、现场损失是什么、先兑什么、再欠什么。
  - P4 `创作蓝图包` 当前批集纲必须吸收转译卡，但不展示内部表格；用户看到的是已经成立的新壳原生强动作和可见现场损失。
  - P5 `场景施工稿` 增加输入完整性判断：如果 P4 高刺激节点仍是冷方案、制度方案、证据方案，或源本动作外形回流，P5 不得继续扩写成正文。
- 当前状态：机制已接入候选合同，但尚未重新跑样本验证，不能视为 workflow pass。

## 2026-07-03：V58 阿尔法样本强桥段转译验证

- 新增阿尔法样本验证目录：`v2-restart/样本验证/阿尔法样本/V58_alpha_v20_strong_bridge_runner/`。
- 本轮按 v20 / v3 的强桥段转译补丁运行，产物已覆盖 `P0-P11` 与 `run_log`，并完成干净 reviewer 独立审稿。
- clean reviewer verdict：`conditional_pass`。V58 明显强于 V40，尤其 EP4-6 和 EP10 已从“危机说明”推进到可见动作链；洗稿边界也比 V40 更干净，没有回到狼人、命定伴侣、狗链、赌场、兔女郎等源本外形。
- 但 V58 仍不能判 full pass，也不能升正式候选。核心硬伤是：EP7 提前回答“孩子是否保住”，削弱 EP7->EP8 付费窗口；EP8-10 医疗抢人链条让 Hale 保护被切开得太容易，追看强度仍低于 V5 / 商业稿。
- 当前横向判断：`V58 > V40`；`V58 洗稿正确性 > V5`；`V58 付费刺激强度 < V5`；距商业稿仍有场面密度差。
- 根因收窄：强桥段转译方向成立，问题不在 P2，不在 P7 英文 polish，而在 P4/P5 交界。P4 把 EP6 救场、EP7 胎心确认、EP8 医疗转运拆得太早；P5 没有把 EP8-10 写成“保护被逐轮剥掉”的多方压迫行为链。
- 下一步若继续验证，应开 `V59`，只回 P4/P5 最小修 EP6-10：延后孩子安全答案，把医疗转运改成多方抢位 / 反咬 / 保护链被剥夺 / Julian 被迫做错选择。不要直接修 P7 正文，不要重写全链路。

## 2026-07-03：商业项目反推，停止 V59 式局部补丁

- 新增支撑审计：`v2-restart/支撑审计/2026-07-03_商业项目反推与升级方向.md`。
- 对 V58 后的下一步判断做修正：不要继续开 `V59` 去修单个付费窗口。那会延续 `V53 -> V58` 的局部补丁循环。
- 当前更准确的方向是反推商业项目《暗涌》的**产物形态和销售密度**，而不是复刻它的剧情外形、枪、车祸、跪地、黑帮等强刺激壳。
- 商业稿真正强在：信息流先卖项目，故事梗概压全剧追看，人物设定绑定欲望 / 罪责 / 关系债，粗纲承担阶段销售，集纲已经像小剧本，正文只是把已足够强的集纲拍出来。
- 本项目应升级的不是“更像商业稿剧情”，而是：`商业项目强度 + 洗稿边界 + 源本有效性继承 + 用户收费边界 + 新壳原生强动作 + 长剧多批次连续性`。
- 下一步应重写候选 workflow 的生产范式，不新增工位：把 P4 从普通 `创作蓝图包` 升级为用户可见的 `商业项目包`，增加信息流 / 项目销售句，要求全剧粗纲和当前批集纲达到商业稿密度；P5 降级为场景拆解层，只把已经足够强的 P4 拍成可写场面，不再补商业逻辑。

## 2026-07-03：商业项目包生产范式接入 v20 / v3

- 已更新 `workflow_spec_v20_产品契约编译候选版_2026-07-03.md` 与 `skill_chain_spec_v3_产品契约链候选版_2026-07-03.md`。
- 本轮仍不改 `PRD_v4`，不新增工位，不新增 reviewer，不改样本文本。
- 核心改法：P4 从普通 `创作蓝图包` 升级为 `商业项目包 / 创作蓝图包`，固定包含 `信息流 / 项目销售句 -> 故事梗概 -> 人物设定 -> 全剧粗纲 -> 当前批集纲 -> 当前批状态与禁区说明`。
- P4 新要求：信息流必须说明本剧靠什么卖；故事梗概要成为全剧销售压缩包；人物设定必须绑定欲望 / 罪责 / 关系债 / 抵赖能力 / 长线用途；全剧粗纲承担阶段销售；当前批集纲必须达到商业稿集纲密度，不能把关键压迫留给 P5 临场补。
- P5 新定位：降级为场景拆解层，只把 P4 已经足够强的集纲拆成可拍、可演、可扩写的场面流；不得重新设计商业卖点、付费窗口或“这一集为什么追 / 为什么付费”。
- 新停线条件：若 P4 缺项目销售句、阶段销售力、当前批小剧本密度、新壳原生强动作、可见现场损失或下一债，P5 不能用新增剧情事实补齐，只能记录 `P4 commercial package too thin`。
- 当前下一步：先做静态检查，确认入口、workflow 和 skill_chain 口径一致；通过后再用新生产范式重跑样本验证。新验证不是 V59 局部补丁。

## 2026-07-03：V60 / V61 商业项目包生产范式双样本验证

- V60 阿尔法样本目录：`v2-restart/样本验证/阿尔法样本/V60_alpha_commercial_package_runner/`。
- V61 飞书三星样本目录：`v2-restart/样本验证/飞书三星样本/V61_feishu_commercial_package_runner/`。
- V60 结论：商业项目包生产范式让阿尔法样本保住 V5 体验信号，但仍未打平成熟商业稿；clean reviewer 与主控均判 `conditional_pass`。
- V61 结论：同一生产范式在飞书三星样本上能保住 V5 体验信号，但相对商业稿仍不足；clean reviewer 判 `conditional_pass`。
- V61 clean reviewer 核心硬伤：EP7-10 过度依赖系统权限、门禁、徽章、继承椅、控制权转移等机关式反转；Adrian 从真相反噬到签掉 Blackwell 略快；Chloe 后半反派压力下降；Damian EP10 反咬铺垫偏少。
- 当前判断：商业项目包方向应保留为候选增强方向，不应撤回；但 V60/V61 都未达到正式 pass，不能升默认 workflow。
- 根因进一步收窄：P4/P5 必须保持商业项目密度；但 P5 不能只把强动作写成系统机关，必须把权限/徽章/继承椅/控制权反转前后压成真人对抗、反派反扑、公开关系撕裂和现场代价。
- 当前下一步：不要继续磨 V61 全文，也不要扩规则。若开 V62，只小回 P5 的 EP7-10，重写后半场景施工，再让 P6/P7 只吃新 P5 重写后半；最终判断只横向对比 V5 / 商业稿，不再用 V61 或 V56 当主基准。

## 2026-07-03：shortdrama-remix 接入“源本赚钱功能 -> 新壳刺激动作”生产链

- 本轮不修改单一样本文本，不新增工位，不新增 reviewer，不回外层 v20 继续堆规则。
- 已直接修改 `shortdrama-remix` 当前主执行链：
  - `skills/source-import/SKILL.md`
  - `skills/short-drama-write/SKILL.md`
  - `contracts/short_drama_form_lock_v1.md`
  - `shortdrama-remix/README.md`
- 根因判断：direct run 能跑完整正文，但仍可能“洗得干净、逻辑成立、短剧刺激不够”。缺口不是某一集要补，而是生产链没有把源本每集为什么赚钱，强制编译成新壳里的可见刺激动作。
- 已接入的核心机制：
  - 源本导入阶段必须拆 `源本赚钱功能 / 即时刺激动作 / 必须迁移的刺激 / 禁止迁移的表层`。
  - 写稿交接包必须包含 `源本商业功能到新壳刺激动作表`。
  - `/plan` 必须做 `新壳承载压力测试`，判断新壳是否能承载同等级羞辱、压迫、反杀、火葬场和付费钩子。
  - `/outline` 不再是一句话分集目录，而是每集必须给出 `源本赚钱功能 / 新壳可见刺激动作 / 反派新伤害 / 本集兑现 / 下集债务 / 结尾按钮`。
  - `/episode` 必须按动作链写正文，不能用证据、文件、系统、会议、鉴定、录音替代现场刺激。
- 当前状态：执行链已改完，但尚未按新版重新跑样本验证。下一步应直接用更新后的 `shortdrama-remix` 跑首批样本，并横向对比 V5 / 商业稿；不能用这次文档修改本身宣称质量通过。

## 2026-07-03：V64 / V65 shortdrama-remix 生产链验证

- V64 验证目录：`shortdrama-remix/新剧/V64_money_function_chain_validation/`。
- V64 结论：对 V5 通过，对商业稿未完全通过。根因不是缺短剧动作，而是新壳动作仍有港口、签字链、经营权等规则理解成本，观众需要多理解一层才知道谁受伤、谁失权、谁欠债。
- 已从 V64 根因继续上修 `shortdrama-remix` 生产链：
  - `source-import/SKILL.md` 增加 `一眼可懂代价`。
  - `short-drama-write/SKILL.md` 要求 `/plan`、`/outline`、`/episode` 都落实 `一眼可懂代价`。
  - `short_drama_form_lock_v1.md` 增加 `代价不可见` 失败类型。
- V65 验证目录：`shortdrama-remix/新剧/V65_one_glance_cost_validation/`。
- V65 结论：对 V5 通过，对商业稿通过，综合通过。
- V65 通过原因：它把主刺激从规则型动作压成观众一眼能懂的代价，例如钥匙手环被摘、玻璃门锁死、血从门下流出、家族牌匾被拆、奖杯砸碎、胸花被撕、腕带粉碎、旧病房胎心停止、男主当众划掉姓氏、逼写母亲名字。
- 当前判断：`源本赚钱功能 -> 新壳等价刺激动作 -> 一眼可懂代价 -> 反派主动新伤害 -> 本集兑现 -> 下一债务` 是当前最有效的生产链补丁。它在飞书三星样本上通过横向对比；是否跨题材稳定，仍需第二题材复验，不能只凭 V65 宣称全项目最终稳定。

## 2026-07-03：V66 阿尔法第二题材复验通过

- V66 验证目录：`shortdrama-remix/新剧/V66_alpha_one_glance_cost_validation/`。
- 第二题材源本：`input/feishu-base-samples-2026-06-28/[修改版]阿尔法的命定孕妻-1-31集-中英台词.txt`。
- V66 结论：第二题材样本通过；对 V60 通过；对 V5 体验线通过；对商业稿要求通过；综合通过。
- V60 的最上层硬伤是 EP8-10 仍有协议、转运、法定身份、医疗流程的冷感，观众需要理解规则才知道 Hale 保护为什么失效。
- V66 修法是把保护失效写成一眼可懂代价：保护手环被剪断、守卫胸牌被摘掉、病床被推出 Julian 手心、门在他面前锁死、Mia 用血写 H、Julian 被车拖伤但仍抓不住。
- V66 没有回流源本禁用外形：不使用狼人、Alpha、命定伴侣、月圆、诅咒、狗链、宠物陪玩、地下赌场、兔女郎、赌桌鞭打、血脉感应或巨狼追车。
- 当前阶段性判断：V65 飞书三星样本通过，V66 阿尔法第二题材样本通过，因此新版 `shortdrama-remix` 生产链可以判为阶段性通过。通过链路是 `源本赚钱功能 -> 新壳等价刺激动作 -> 一眼可懂代价 -> 反派主动新伤害 -> 本集兑现 -> 下一债务`。
- 下一步应收口入口、整理可提交状态并提交；不要继续打单一样本文本补丁。

## 2026-07-03：V64 书写层隔离实验反推 P5/P6/P7 替换

- 新增隔离实验：`v2-restart/样本验证/飞书三星样本/V64_writer_layer_ep1_3_pass_attempt/`。
- 结论：EP1-3 书写层隔离验证为 `pass`，但仅证明飞书三星样本前 3 集的书写层修法成立，不代表正式 workflow 全链路通过，也不代表全剧稳定。
- 已按该实验反推更新 `workflow_spec_v20_产品契约编译候选版_2026-07-03.md` 与 `skill_chain_spec_v3_产品契约链候选版_2026-07-03.md`。
- 替换内容不是新增 reviewer 或新增规则，而是替换 P5/P6/P7 的执行姿势：
  - P5 主体产物从剧情清单 / 场面流，改为逐场 `scene card + scene draft seed`。
  - P5 每场必须执行：场上目标、阻挡动作、开场局面、变化点、一眼可懂代价、对白承载、结束局面。
  - P6 必须把 P5 逐场材料写成镜头化、动作化、反应链化 screenplay，抽象信息必须转成可见物件或可见代价。
  - P7 必须按场修 surface speech / real objective、潜台词、声线、动作承载和表达顺序，不能退化成逐句美容。
- 当前下一步：按已替换的 P5/P6/P7 跑飞书三星完整 EP1-10 隔离样本。只有完整 EP1-10 对 V5 / 商业稿 / 洗稿边界均通过，才允许继续判断是否升正式候选。

## 2026-07-03：V66 独立审稿落盘与 Archmage 非同型样本执行

- V66 阿尔法 writer-layer 样本已补充 clean reviewer 独立审稿：`v2-restart/样本验证/阿尔法样本/V66_alpha_v20_writer_layer_ep1_10_runner/04_clean_reviewer_独立审稿.md`。
- 独立审稿结论：`pass`。它只证明 V66 作为阿尔法 writer-layer 样本过线，不等于把 v20 默认流程直接升正式。
- 为避免只在阿尔法 / 飞书三星两类题材上自证，已按当前 `shortdrama-remix` 主链跑一个非同型样本：`shortdrama-remix/新剧/The_Sun_Core_Heiress/`。
- 非同型样本源本库：`shortdrama-remix/源本库/Archmage_Golden_Sun_Warrior/`，已拆出 30 集原文，并产出源本有效性、事件账本、人物功能、爽点钩子、付费压力、可迁移结构、禁抄边界、新壳迁移建议和写稿交接包。
- 新剧产物已完整落盘：`run_log.md`、`source-handoff.md`、`creative-plan.md`、`characters.md`、`episode-directory.md`、`episodes/ep001.md` 至 `episodes/ep010.md`、`review/EP1-10_self_review.md`。
- 静态检查结果：10 集正文齐；每集 3 场；每集均超过 600 English words；每集均含 `End Hook` 和 `Next`；正文 / 蓝图 / 自检未命中源本禁抄词和阿尔法污染词。
- clean reviewer 已独立审 `The_Sun_Core_Heiress`，结论写入：`shortdrama-remix/新剧/The_Sun_Core_Heiress/review/EP1-10_clean_reviewer.md`。
- clean reviewer verdict：`pass`。短剧感成立，EP1-3 追看链条顺，EP8-10 付费冲动更强；救全族、打跪 Rook、Sable 瞄准母亲、Mira 用身体认女、Nova 被冷核矛击中后变异悬置，都是动作 / 关系 / 生死断点，不是文件悬念。
- clean reviewer 硬伤：EP4 最弱且有流程化风险；`Solar Core / cold-core / dual command` 后续不能继续加解释；对白有效但爆款记忆点还可以更短、更毒。
- 当前判断：`The_Sun_Core_Heiress` 已证明新版主链可以把一个魔法家族强者逆袭源本迁移成科幻机甲贵族新壳，并形成完整 EP1-10 正文；非同型样本通过。下一步不应继续打地鼠修本样本，应收口当前证据，决定是否再跑第二个更远题材复验或进入提交整理。

## 2026-07-04：PRD 同步到多 agent 产品架构视角

- 已同步 `v2-restart/PRD_v4.md`，保留其产品定义定位，但把执行入口从历史 v20 / v3 workflow 口径切到当前 `shortdrama-remix` 主链。
- 最终产品形态明确为三层：用户层由主控 agent / 未来网页入口承接；生产层由 `source-import`、`short-drama-write`、clean reviewer 和按需 helper agent 组成；能力层保留 skills、references、contracts 和 state。
- 当前判断：不要把所有外部短剧方法继续塞进一个巨型 workflow。外部 skill、reference、题材包或 helper 输出如果要进入产品，就必须落到用户可确认蓝图、writer 必须消费的分集执行包，或 reviewer / polish 的硬边界。
- PRD 已把蓝图定位为“用户真实理解 + writer 主上游”：信息流、人物设定、全剧粗纲、当前批集纲、当前批状态与禁区都应写成自然语言小 draft；背后再用硬执行锚点增强 writer 执行，不做两套互相漂移的剧情。
- 下一步方向：先以 `shortdrama-remix` 收口第一条可执行生产链；只做架构感最小增强；长线多批次状态包单独设计，不挤进首批主链；没有实质链路修改时，不再为单一样本继续重跑。

## 2026-07-04：shortdrama-remix 架构对齐改造

- 新增检查点文档：`shortdrama-remix/architecture_alignment_checkpoint_2026-07-04.md`，明确视听语言、首批强度地板、角色状态、信息释放、蓝图、正文写法、reviewer、callback、clean run 和长线多批次状态包分别落到哪里，防止上下文压缩后丢失。
- 已更新 `shortdrama-remix/README.md`：明确本目录是最终产品架构里的生产层，不是方法库；新增三层架构、三类有效接入物，以及当前不回旧 workflow、不修旧样本文本的执行原则。
- 已更新 `contracts/short_drama_form_lock_v1.md`：把旧 `场景施工稿` 口径替换为 `分集执行包 / 当前批集纲`；增加人物状态、视听语言、信息释放梯度、reviewer 严格度、callback 和 polish 权限边界。
- 已更新 `skills/source-import/SKILL.md`：源本账本和写稿交接包增加信息释放 / 继续扣住、人物状态压力、视听承载特征，避免 writer 只收到剧情摘要。
- 已更新 `skills/short-drama-write/SKILL.md`：`/plan` 产出自然语言商业项目包小 draft；`/characters` 锁定欲望、关系反应、声线和首批状态；`/outline` 升级为分集执行包；`/episode` 增加视听语言、反应链和有限去 AI 味 polish；`/review` 增加硬伤归因和有限 callback；新增 `/batch-state` 作为长篇多批次状态包接口。
- 当前判断：这轮是主链产品化硬化，不是新 workflow。下一步如要验证，应按改后的 `shortdrama-remix` 做 clean run，并留下 `run_log.md`；否则先收口版本，不继续空跑样本。

## 2026-07-04：外部参考最低必要接入审计

- 新增审计文档：`shortdrama-remix/external_reference_minimum_audit_2026-07-04.md`。
- 本节为早一轮判断，后续已被“长线状态与视听表演接入 short-drama-write”修正：`scene-action-brief.md` / `dialogue-subtext-brief.md` 不再单独创建，相关能力合并进 `performance-dialogue-brief.md`；长线状态能力单独落为 `series-state-brief.md`。
- 本轮区分两类“按需接”：已接入但按阶段读取的 `short-drama` 标准库；尚未正式接入但可评估后接的 `how-to-make-script` / `oh-story` 局部文件。
- 已确认：`short-drama` references 已经是当前主链的一部分，不需要重新接，只需验证执行质量。
- 当时决策：只接 `how-to-make-script` 的 `scene-writing` 和 `dialogue-subtext` 瘦身版，做成本地 `scene-action-brief.md` 和 `dialogue-subtext-brief.md`，分别供 `/episode` 和 `/review` 按需读取；后续已修正为合并进 `performance-dialogue-brief.md`。
- 决策：不整接 `oh-story` 的 `story-deslop`、`story-review`、`story-short-write`。原因是这些是网文 / 小说口径，完整接入会污染短剧正文、reviewer 和上下文注意力。`oh-story` 状态追踪只保留在 `/batch-state` 接口里，长线多批次阶段再决定是否扩展。
- 当时下一步：先完成上述两个瘦身 reference 的最小接入，再跑 clean run；后续实际执行已改为 `series-state-brief.md` + `performance-dialogue-brief.md`。

## 2026-07-04：外部候选完整阅读边界修正

- 已修正 `shortdrama-remix/external_reference_minimum_audit_2026-07-04.md` 的“读过”口径：只有主控本轮完整打开并读完的文件，才标为已读；摘要、目录扫描、候选名出现都不算已读。
- 已完整复读当前接入面：`README.md`、`short_drama_form_lock_v1.md`、`source-import/SKILL.md`、`short-drama-write/SKILL.md`、本地 8 个 `short-drama` reference，以及 `vendor/short-drama/SKILL.md`。`vendor/short-drama/references/` 与本地 8 个 reference 已确认完全一致。
- 已完整读完本轮可接候选：`how-to-make-script` 的 `scene-writing` / `dialogue-subtext` 两个 skill 及其 workflow/rubric 共 6 个文件。
- 已完整读完并排除：`oh-story/story-deslop` 主文件、`anti-ai-writing.md`、`banned-words.md`，`story-review/SKILL.md`，`story-short-write/SKILL.md`，以及状态追踪 demo 的 `角色状态.md`、`伏笔.md`、`上下文.md`、`时间线.md`。
- 明确未读且不得抽取：`story-review/references/`、`story-review/scripts/`、`story-short-write/references/`、`story-short-write/scripts/`、`story-deslop/scripts/`。这些不进入本轮候选；后续若要抽其中任意文件，必须先全文读取再判断。
- 已更新 `shortdrama-remix/README.md`，消除“直接跑 vendor 原入口”的歧义：当前执行只跑本地改造副本，`vendor/` 只作冻结来源和追溯证据。

## 2026-07-04：长线状态与视听表演接入 short-drama-write

- 新增 `shortdrama-remix/skills/short-drama-write/references/series-state-brief.md`：把长线续写能力改成本项目自己的轻量执行 brief，不整接网文/小说长篇工作流。
- 新增 `shortdrama-remix/skills/short-drama-write/references/performance-dialogue-brief.md`：把视听表演、反应链、台词议程和轻 polish 变成 `/episode` 与 `/review` 的执行依据，不作为泛参考。
- 较早审计里计划拆成 `scene-action-brief.md` / `dialogue-subtext-brief.md` 的内容已合并进 `performance-dialogue-brief.md`，避免 reference 过碎导致 writer 注意力分散。
- 已更新 `shortdrama-remix/skills/short-drama-write/SKILL.md`：
  - `/plan` 必须从自然语言蓝图里建立全剧追踪骨架，首批 1-10 集不能被当成孤立样片。
  - `/characters` 必须承接欲望、恐惧、面具、关系反应和不能倒退项，但不能写成固定模板。
  - `/outline` 必须让每集能抽出本集速记、上一集状态承接、不可提前消费和状态增量目标。
  - `/episode` 必须读取 `series-state-brief.md` 与 `performance-dialogue-brief.md`，正文要写出反应链、视听承载、台词议程，并在每集末尾追加简短 `状态增量`。
  - `/review` 必须按同一套 brief 归因问题，区分蓝图层、角色层、执行层、台词层；同一问题最多一次上游返修，禁止无限 callback。
  - `/batch-state` 已改为“批次完成后的续写状态汇总”：长线追踪从 `/plan` 开始，`batch-state.md` 只在首批完成或继续下一批前汇总已确认事实。
- 禁抄和洗稿口径保持不变：不使用机械字符串黑名单；常见人名、短句套路和通用短剧话术可以继续使用，只有高识别表层组合复刻才构成硬风险。
- 当前状态：这是主链执行能力接入，不是 clean run 结论。下一步如果验证质量，必须按新版 `shortdrama-remix` 跑干净样本并留下 `run_log.md`。

## 2026-07-04：V68 新版主链 clean run 通过

- clean runner 输出目录：`shortdrama-remix/新剧/V68_clean_series_performance_run/`。
- 源本库：`shortdrama-remix/源本库/黑手党少主痛哭求原谅_前妻她杀疯了/`。
- runner 按当前主链执行到 `/review 1-10`，产出 `run_log.md`、`source-handoff.md`、`creative-plan.md`、`characters.md`、`episode-directory.md`、`episodes/ep001.md` 至 `ep010.md`、`review/EP1-10_self_review.md`。
- 机械完整性检查：10 集正文齐；每集都有 `## 状态增量`；每集均超过 600 English words；`run_log.md` 明确记录已读取并执行 `series-state-brief.md` 和 `performance-dialogue-brief.md`。
- clean reviewer 报告：`shortdrama-remix/新剧/V68_clean_series_performance_run/review/EP1-10_clean_reviewer.md`。
- clean reviewer verdict：`pass`。它可以作为新版主链 clean run 的质量证据，因为正文把源本赚钱功能迁移成了新壳下的可见短剧动作，而不是 workflow 字段堆叠。
- 通过点：EP1-10 有可见冲突、当场损失、关系/权力变化和结尾债务；源本的夺位、救错、失子、回归、女配反杀、男主同罪、火葬场、交权不原谅都落到了医疗慈善信托新壳动作上。
- 洗稿判断：没有回流黑手党、枪、火海、亲子验血、保险箱小毛衣、枪口测试等高识别组合；相似性停留在允许保留的抽象商业功能。
- EP7-8 专项：虽有 proof-flow / hearing 外形，但核心动作是门牌、站位、胸牌、投票平板等现场权力剥夺，未滑成文件推进；只建议最终商业稿前压缩 10%-15% 的证据解释句。
- 当前判断：新版主链可以标记为 `clean run pass / 可作为主链质量证据 / 仍需轻 polish 后再当最终商业稿`。不需要回 `plan`、`characters` 或 `outline` 级返工。

## 2026-07-04：台词精修升级为固定中间环节

- 用户纠正：最终商业稿清修不应是针对 V68 单一样片修稿，而应该升级为项目层通用能力；未来会有很多不同剧本，必须把台词环节做对。
- 新增 `shortdrama-remix/skills/short-drama-write/references/dialogue-polish-brief.md`。
- 台词 brief 吸收 `dialogue-subtext`、`wp-dialogue-polish`、`rb-dialogue`、`dialogue-mastery` 和 `short-deslop` 中适合短剧的部分：表层话 / 真实目标、声线差异、权力句长、解释压缩、反应拍、去 AI 味。
- 仍不整接外部 `how-to-make-script`、`story-short-write`、`story-deslop` 或 `story-review`。外部能力只以本地 brief 形式进入当前产品，避免小说链、机械禁词和泛写作规则污染短剧主链。
- 已更新 `shortdrama-remix/skills/short-drama-write/SKILL.md`：主链改为 `/write-from-source -> /plan -> /characters -> /outline -> /episode -> /dialogue-polish -> /review`。
- `/dialogue-polish` 权限被锁死：只能压台词、删解释、调整声线、补少量反应拍和去 AI 味；不得改剧情事实、付费窗口、人物关系、真相释放、本集核心代价或状态增量事实。
- `/review` 默认认为正文应先经过 `/dialogue-polish`；若未执行，只能把台词问题归到台词层，不能误判为蓝图失败。
- `/export` 增加交付清理：必须移除 `Dialogue Polish Notes`、`状态增量` 和商业执行字段，不把内部 workflow 痕迹交给用户。

## 2026-07-04：产品入口升级为 main controller 与 rewrite 用户命令

- 用户确认：本项目面向 Codex 内使用，不做网页 App，不要求新用户安装额外全局 skill。
- 新增项目内主控执行文件：`shortdrama-remix/skills/shortdrama-main-controller/SKILL.md`。
- 新增用户层命令别名：`/rewrite-start`、`/rewrite-blueprint`、`/rewrite-write`、`/rewrite-polish`、`/rewrite-review`、`/rewrite-continue`、`/rewrite-export`、`/rewrite-status`。
- `/rewrite-*` 是用户层别名，不重命名底层内部命令；内部仍使用 `/write-from-source`、`/plan`、`/characters`、`/outline`、`/episode`、`/dialogue-polish`、`/review`、`/batch-state`、`/export`。
- 新增 `QUICKSTART.md` 和 `docs/产品架构与主控路由_v1.md`，把 GitHub clone 后的 Codex 使用方式、主控调度、确认点、多 agent 边界和多批次规则固定下来。
- 已重写根目录 `README.md`，删除把 v20 / v3 作为当前入口的过期说明；当前用户入口改为自然语言触发 main controller。
- 当前判断：这是产品入口和路由层改造，不是剧本质量 clean run。验收重点是冷启动演练：新用户只读 README/QUICKSTART 后，主控能否进入当前主链、避免旧链、正确等待蓝图确认，并知道首批 1-10 不是孤立样片。

## 2026-07-04：外部 worktree 文件夹收口

- 已确认 Finder 里并列出现的 `自动化编剧-*` 文件夹是 Git worktree，不是应该搬进主项目的普通目录。
- 已在移除前完整保全五个外部 worktree：`clean-workflow-validation`、`county-workflow-synthesis`、`external-chain-rebuild`、`remix-synthesis-validation`、`v49-worktree`。
- 归档位置：`.local-archive/obsolete-worktrees-2026-07-04/`。其中 `_meta/` 保存每个 worktree 的分支、HEAD、status、tracked diff、untracked 清单和归档文件列表。
- 已用 Git worktree 正规移除外部文件夹并 prune；当前 `git worktree list` 只剩 `/Users/jiakun/Codex/自动化编剧`。
- 本轮不删除本地 `codex/*` 分支标签。判断：Finder 杂乱已经解决，分支标签先保留作历史保险；后续如果要 Git 分支也瘦身，再单独清理。

## 2026-07-04：冷启动演练补齐 run_log 硬证据

- 本轮按 `codex/next` 开发接手入口继续推进，选择冷启动演练而不是继续跑旧样本。
- 静态检查发现：README / QUICKSTART / 主控都要求“按当前主链执行必须留下 `run_log.md`”，但 `short-drama-write` 的工作目录和各内部命令没有把 `run_log.md` 固定为一等产物。
- 已更新 `shortdrama-remix/skills/short-drama-write/SKILL.md`：项目目录新增 `run_log.md`，并要求 `/start`、`/write-from-source`、`/plan`、`/characters`、`/outline`、`/episode`、`/dialogue-polish`、`/review`、`/batch-state`、`/export`、`/compliance` 每步完成或中止时都追加运行记录。
- 已更新 `shortdrama-remix/skills/shortdrama-main-controller/SKILL.md`：主控在生产项目开始时必须检查或创建 `run_log.md`；若产物存在但运行日志缺失，只能报告“运行证据缺失”，不能宣称完整按当前主链执行。
- 已更新 `docs/产品架构与主控路由_v1.md`：冷启动验收增加 `run_log.md` 状态检查。
- 当前判断：这是入口可信度修补，不是剧本质量验证；不需要重跑样本。下一步仍可继续做冷启动演练，或转入 `/dialogue-polish` clean run 验证。

## 2026-07-04：Issue 模板与发布流程补齐

- 本轮继续 `codex/next` 开发接手入口里的第 4 项：让 GitHub 用户反馈能进入 `codex/issue-*` 分支。
- 新增 `.github/ISSUE_TEMPLATE/bug_report.yml`：用于当前 `shortdrama-remix` 主链运行问题，要求区分责任层、期望/实际结果、证据路径和 `run_log.md`。
- 新增 `.github/ISSUE_TEMPLATE/product_feedback.yml`：用于入口、流程、质量标准、续批、导出或文档层面的产品反馈。
- 新增 `.github/ISSUE_TEMPLATE/config.yml`：关闭空白 issue，减少空泛反馈。
- 新增 `docs/issue与发布流程.md`：固定 issue 进入规则、`codex/issue-{number}-{slug}` 分支规则、分层修复边界、验证规则、合回 main 标准和发布检查清单。
- 已更新 `README.md`、`QUICKSTART.md`、`docs/开发接手入口.md`，把 issue / release 入口指到新流程文档。
- 当前判断：这是仓库治理和用户反馈入口补齐，不是主链质量验证；验证方式是 YAML 可解析、入口文档可找到、规则不引导用户复活旧 workflow。

## 2026-07-04：出海本地化适配保守护栏接入主链

- 用户明确：输入稿默认已经洗过一轮，且大概率也做过一轮本地化；系统不能默认它是原始国内稿，也不能默认它适配失败。
- 本轮不把散落资料揉成大百科，也不新增一套爽点/付费/题材系统。`genre-guide.md` 继续负责题材映射、爆款元素和文化符号，`satisfaction-matrix` / `paywall-design` / `rhythm-curve` 继续负责短剧爽点、付费和节奏。
- 新增 `shortdrama-remix/skills/short-drama-write/references/overseas-localization-brief.md`：只做保守本地化复核、证据化建议、目标语境承载和误伤检查；不自动选题，不自动深改。
- 已更新 `source-import`：目标市场为出海或未定时，先复核现稿已有适配质量，记录已经成立的本地化处理、表层本地化风险、高置信调整建议、必须保护的有效点和证据不足暂不建议改的位置。
- 已更新 `short-drama-write`：`/plan` 成为唯一方向决策层；本地化建议必须带文本证据、推理链路、置信度、建议级别、预期收益、误伤风险和用户确认状态；`/outline`、`/episode` 只执行已确认策略；`/review` 检查表层替换、过度改写、无证据建议和未确认重构。
- 已更新 `shortdrama-main-controller` 与形态锁：目标平台/地区仍是需求收集项，但 agent 权限降为“适配风险诊断 + 方案建议”；用户是最终拍板人，除非明确授权 agent 决定方向。
- 当前判断：这是“少改但改准”的护栏，不是出海自动重构引擎。它解决的是“如何本地化适配不同平台/观众，同时不误伤已经有效的源稿”。
